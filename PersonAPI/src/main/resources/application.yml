spring:
 datasource:
    url: jdbc:mysql://localhost:3306/person_db01?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
 jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true

application:
    name: PersonAPI

server:
  servlet.context-path: /person/v1
    
springdoc:
  swagger-ui:
    use-root-path: true